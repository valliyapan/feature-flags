Feature flags dashboard

How to run the project locally:

1. Make sure Node.js (version 22 or higher) and npm are installed
2. Open terminal and run `npm start-backend`
3. Open another terminal and run `npm start-frontend`
4. Open http://localhost:4000 in your browser